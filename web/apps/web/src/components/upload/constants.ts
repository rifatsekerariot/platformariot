/**
 * Default minimum file size (in bytes)
 */
export const DEFAULT_MIN_SIZE = 0; // 0B

/**
 * Default maximum file size (in bytes)
 */
export const DEFAULT_MAX_SIZE = 1024 * 1024 * 10; // 10MB

/**
 * Default maximum parallel number of uploading files
 */
export const DEFAULT_PARALLEL_UPLOADING_FILES = 5;
